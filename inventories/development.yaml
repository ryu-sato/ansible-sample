all:
  children:
    rails_servers:
      hosts:
        localhost:
          ansible_connection: local
          ansible_become: yes
          ansible_become_method: sudo
          ansible_become_flags: --login
  vars:
    # ruby_version: 3.0.0
